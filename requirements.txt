# Requirements for the CeNN-QML-TSF proof-of-concept benchmark
# Reported execution environment:
#   Python 3.12.12
#   Kaggle notebook environment
#   GPU: NVIDIA Tesla T4
#   PyTorch: 2.10.0+cu128
#   NumPy: 2.0.2
#   Pandas: 2.3.3
#
# Note:
# Kaggle already provides most of these packages. This file is included
# for transparency and controlled replication outside the original session.

numpy==2.0.2
pandas==2.3.3

# PyTorch used in the reported Kaggle run was 2.10.0+cu128.
# If this exact CUDA build is unavailable on another machine, install the
# closest compatible PyTorch build for your CUDA/CPU environment.
torch==2.10.0

scikit-learn>=1.5.0
scipy>=1.14.0
statsmodels>=0.14.0

matplotlib>=3.9.0
tqdm>=4.66.0

# Used when downloading datasets through Kaggle/KaggleHub workflows.
kagglehub>=0.3.0

# Optional but useful for YAML/JSON configs and notebook execution.
PyYAML>=6.0.0
jupyter>=1.0.0
ipykernel>=6.29.0
