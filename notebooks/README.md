# Notebooks

- `reproduce_results_from_outputs.ipynb`: loads the exported CSV files and article figures from `outputs/full_run/` for reviewer inspection.
- `neurosymbolic-cenn-qml-tsf-benchmark.ipynb`: benchmark notebook scaffold. Replace this file with the exact original Kaggle/Colab notebook if available.

The uploaded `results.zip` did not contain an `.ipynb` file. It contained exported outputs, figures, manifests, metadata and prediction CSV files.
